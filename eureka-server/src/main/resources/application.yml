server:
  port: 10086
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false # 将自己注册上去
    fetch-registry: false # 拉取服务
    service-url:
      defaultZone: http://localhost:10086/eureka
